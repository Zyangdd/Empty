<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="EmptyAppTheme">
        <item name="android:windowBackground">@drawable/bg_window</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>